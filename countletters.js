const letterscount = function(allItems) {
    const letterscount = object;
    for (const item of allItems) {
        letterscount(letter[ = letterscount[letter] + 1 || 1;
    }
}  
 return letterscount;

