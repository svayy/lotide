const countOnly = function (allItems, itemsToCount) {
    const resultobj = {};
    for (const objectof allItems) {
      if (itemsToCount[object]) {
        if (resultobj[object]) { resultObj[object] += 1;
        else resultObj[object] = 1;
      }
    }
    console.log(resultObj);
    return resultObj;
  };


    
